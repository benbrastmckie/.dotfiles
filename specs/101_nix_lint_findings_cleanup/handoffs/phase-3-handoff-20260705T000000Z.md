# Phase 3 Handoff — Task 101

**Immediate Next Action**: Begin Phase 4 (hand-collapse W20 Tier 1 files: flake.nix, lib/mkHost.nix,
hosts/iso/default.nix, hosts/usb-installer/default.nix, home.nix, modules/system/audio.nix,
modules/system/services.nix, modules/home/email/aerc.nix).

**Current State**: Phases 1-3 completed. Plan file is current with [x] annotations and verification
notes. `nix flake check` green, `statix check` shows only 18 W20 findings remaining (zero
W10/W04/W08), `deadnix --exclude 'hosts/*/hardware-configuration.nix' . -o json` reports 0.

**Key Decisions Made**:
- Skip-comment placement: `# deadnix: skip` goes on the line immediately above the flagged
  binding (header line, or the `let`-bound override-callback line for python-packages.nix).
- deadnix `--exclude` flag ordering matters: flags (including `--exclude <glob>`) must precede the
  positional file-path argument (`deadnix --exclude 'glob' .`), not `-- .` after other flags — the
  `-- .` form did not reliably apply the exclusion in this deadnix version.

**Deviations from Plan**:
- Phase 2: `deadnix --edit --exclude 'hosts/*/hardware-configuration.nix' -- .` transiently
  stripped the `pkgs` arg from all 4 hardware-configuration.nix files despite the exclude flag
  (flag-ordering issue above). Detected via post-edit diff, manually restored all 4 files via
  `git show HEAD:<path> > <path>` before committing. No hardware-configuration.nix file appears in
  the Phase 2 commit's diff.

**What NOT to Try**: Do not use `deadnix --edit ... -- .` (trailing `-- .` after `--exclude`) and
expect the exclude glob to be honored — always verify with a dry-run (`-o json`, flags-then-path
order) before trusting `--edit` near the hardware-config files.

**References**: Plan at specs/101_nix_lint_findings_cleanup/plans/01_lint-findings-cleanup.md,
currently after Phase 3.
