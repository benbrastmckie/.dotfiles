# Phase 1 Handoff: Task #93

**Immediate Next Action**: Begin Phase 2 (update `README.md:188` and `docs/development.md`
near line 71 with the opt-in checkpoint language / flag cross-reference).

**Current State**: Phase 1 completed. `scripts/update.sh` rewritten with a single
`for arg in "$@"` flag-parsing loop (`DO_UPDATE`, `DO_NO_CHECK`, `CHECKPOINT`), the checkpoint
gate is now opt-in (`--checkpoint` / `UPDATE_CHECKPOINT=1`, default OFF) and refuses with a
non-zero exit + stderr message on a dirty tree when the opt-in is absent. `git add -A` now only
appears inside the opt-in branch. Plan file phase 1 marked `[COMPLETED]` with all checklist items
checked. Committed as `b517f2c`.

**Key Decisions Made**:
- Kept `SKIP_CHECK` variable assignment as-is even though the two rebuild invocations hard-code
  `--option allow-import-from-derivation false` regardless of `$SKIP_CHECK` — this pre-existing
  quirk is out of scope for task 93 (plan's non-goals: "No changes to the two rebuild invocations
  ... beyond the flag refactor").
- Refusal message names both remediation paths (`--checkpoint` / `UPDATE_CHECKPOINT=1`) plus
  manual commit/stash, per the plan's risk mitigation table.

**Deviations from Plan**:
- None.

**What NOT to Try**:
- Do not restore unconditional `git add -A` behavior — that is exactly the incident this task
  fixes (6ba1f4e, 02f806d).

**References**:
- Plan: `specs/093_update_sh_auto_commit_opt_in/plans/01_checkpoint-opt-in.md`
- Current phase: 1 of 3 (completed); next is Phase 2.
