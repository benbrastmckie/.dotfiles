# Phase 2 Handoff: Task #93

**Immediate Next Action**: Begin Phase 3 (verification: `bash -n`, dirty-tree dry runs,
`--update` path, combined-flag ordering, `nix flake check`).

**Current State**: Phase 2 completed. `README.md:188` corrected to reflect the new opt-in
default (no auto-commit; `--checkpoint` mentioned). `docs/development.md` near line 71 now has a
one-line cross-reference to the README's Full Update section for the flag list. Audited all other
markdown files referencing `update.sh` (docs/installation.md, docs/README.md,
docs/unstable-packages.md, docs/testing.md, docs/usb-installer.md, docs/dual-home-manager.md,
packages/README.md, hosts/README.md, hosts/nandi/README.md) — none assert checkpoint/commit
behavior, confirming the research audit. Committed as `a99e916`.

**Key Decisions Made**:
- Used a relative markdown link `../README.md#full-update-recommended` for the
  docs/development.md cross-reference, pointing at the README anchor generated from the "Full
  Update (recommended)" heading.

**Deviations from Plan**:
- None.

**What NOT to Try**:
- Do not duplicate full flag semantics in docs/development.md — plan explicitly says
  cross-reference only.

**References**:
- Plan: `specs/093_update_sh_auto_commit_opt_in/plans/01_checkpoint-opt-in.md`
- Current phase: 2 of 3 (completed); next is Phase 3 (verification, final).
