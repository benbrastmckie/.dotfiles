# Phase 1-2 Handoff: Task 88 Module Granularity Pass

**Immediate Next Action**: Begin Phase 3 — merge `packages/{fonts,lean-math,ai-tools}.nix` into
a new `packages/misc.nix`.

**Current State**: Phases 1 and 2 completed and committed (`d81cbf8`). Plan file
`specs/088_module_granularity_pass/plans/01_module-granularity-refactor.md` is current, both
phases marked `[COMPLETED]` with deviation annotations on their checklists.

**Key Decisions Made**:
- Baseline closure captured on the truly clean pre-change tree at
  `/nix/store/7zlz05l6ghw72r4sqmsq2hx4ljs2dhh5-home-manager-generation` (via `git stash`/`pop`
  around the Phase 2 edits, since Phase 2 work was briefly done before Phase 1's baseline
  capture — see plan Phase 1 deviation note).
- `agent-tools/lib.nix` kept all four shared helpers (`manifestDirDefault`, `mkPreamble`,
  `mkMutationPreamble`, `lower`) in one plain (non-module) file, per the plan's explicit
  instruction not to split `mkMutationPreamble` further. File is 316 lines (vs. the report's
  ~290 estimate) — treated as acceptable given the "~" qualifier and the Non-Goals constraint.
- Per-binary files use `import ./lib.nix` (no module args) and `inherit` only the helpers they
  need (`mkPreamble` for census/classify/unsubscribe-extract; `mkMutationPreamble` for
  archive-confirmed/delete-confirmed).
- Aggregator entry uses the directory form `./email/agent-tools` (not
  `./email/agent-tools/default.nix`), per the report's recommendation.

**Deviations from Plan**:
- Phase 1 baseline capture was executed after Phase 2 edits were already made; corrected via
  `git stash push -- modules/home/` -> baseline build -> `git stash pop` before any commit, so
  the recorded baseline path is verified correct (captured on a genuinely clean
  `modules/home/`/flake-root tree). See Phase 1 checklist annotations in the plan file.
- Working-tree cleanliness for Phase 1 was scoped to `modules/home/`, `flake.nix`, `flake.lock`,
  `lib/mkHost.nix` rather than the whole repo, since unrelated in-flight tasks (086, 087, 089,
  093, TODO.md, state.json) had pre-existing uncommitted changes outside this task's scope.
- `lib.nix` line count (316) exceeds the report's "~290" estimate by ~9%; not a deviation from
  a hard requirement (the plan's own Testing & Validation line says "~290" with a tilde), but
  noted for transparency.
- `diff-closures` against the Phase 1 baseline was already run informally at the end of Phase 2
  (empty result) even though the plan schedules the authoritative check at Phase 6. This is
  additive verification, not a substitute for the Phase 6 step, which will still run.

**What NOT to Try**:
- Do not re-run the Phase 1 baseline build again — the recorded store path is verified good.
- Do not attempt to further split `mkMutationPreamble` (explicitly out of scope, Non-Goals).

**References**:
- Plan: `specs/088_module_granularity_pass/plans/01_module-granularity-refactor.md`
- Current phase: 2 of 6 (complete); next is Phase 3.
- Baseline store path: `/nix/store/7zlz05l6ghw72r4sqmsq2hx4ljs2dhh5-home-manager-generation`
  (also recorded in `/tmp/task88-baseline-path.txt` on this machine, non-durable).
