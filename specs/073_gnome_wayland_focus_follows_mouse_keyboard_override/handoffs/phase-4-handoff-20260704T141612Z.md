# Phase 4 Handoff — Task #73

**Immediate Next Action**: Phase 5 (behavioral verification) is manual/physically-interactive and cannot be executed by an agent. All automatable work (phases 1-4) is complete and verified. The only remaining action is for the user to run `home-manager switch --flake .#benjamin` is already done; the user must log out and log back in (GNOME Wayland cannot hot-reload a newly-enabled extension), then walk through the Phase 5 manual test checklist in the plan.

**Current State**: Phases 1-4 completed. Plan file `specs/073_gnome_wayland_focus_follows_mouse_keyboard_override/plans/01_mouse-follows-focus-extension.md` is current with inline deviation/verification annotations.

**Key Decisions Made**:
- Package attribute is `pkgs.gnomeExtensions.mouse-follows-focus-2` (not `mouse-follows-focus` as guessed in the plan draft) — `mouse-follows-focus` does not exist in the pinned nixpkgs; `mouse-follows-focus-2` resolves to `gnome-shell-extension-mouse-follows-focus` v12.
- Verified real UUID from the built package's `share/gnome-shell/extensions/*/metadata.json`: `mouse-follows-focus@crisidev.org` (project: `crisidev/mouse-follows-focus`, NOT `LeonMatthes/mousefollowsfocus`).
- `focus-mode = "sloppy"` and `focus-change-on-pointer-rest = false` left byte-for-byte unchanged; `focus-mode = "click"` not applied (per LOCKED decision).
- Ran `home-manager switch --flake .#benjamin` (not just build) since it is safe/additive (dconf + package profile only) and does not require a logout — confirmed live via `gsettings get org.gnome.shell enabled-extensions`.

**Deviations from Plan**:
- Phase 1 task: package attribute name altered from `mouse-follows-focus` to `mouse-follows-focus-2` (the plan's own risk table anticipated needing exact-UUID verification; this extends to needing the exact attribute name too).
- Phase 2: `shell-version` compatibility check FAILED — extension declares `["45","46","47","48","49"]`, installed GNOME Shell is `50.1`. This is a known risk from the plan's risk table (row 2) and is NOT a blocker for this implementation per the user's LOCKED decision to proceed, but it means Phase 5 must specifically re-check whether GNOME Shell 50 refuses to enable the extension due to version mismatch. If it does, a follow-up task will be needed (e.g. nixpkgs override patching metadata.json shell-version, or an upstream extension update).
- Phase 5: all manual/behavioral checklist items deferred to the user — not executed, not fabricated. `gnome-extensions list --enabled` (checked pre-switch, in the still-running GNOME Shell process) does not yet show the extension, which is expected pre-re-login.

**What NOT to Try**:
- Do not attempt to force a GNOME Shell restart on this live Wayland session (would disrupt the user's current desktop) — re-login is the user's responsibility per plan Phase 5.
- Do not fabricate results for the Phase 5 manual tests.
- Do not set `focus-mode = "click"` unless the user explicitly confirms after Phase 5 testing shows the extension approach fails (e.g., due to the shell-version 50 vs 49 mismatch).

**References**: Plan path `specs/073_gnome_wayland_focus_follows_mouse_keyboard_override/plans/01_mouse-follows-focus-extension.md`; current phase 4 (complete); phase 5 pending user.
