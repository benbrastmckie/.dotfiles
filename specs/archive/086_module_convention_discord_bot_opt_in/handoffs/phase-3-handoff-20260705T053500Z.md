# Handoff: Task 86, after Phase 3 (wave 1 complete)

**Immediate Next Action**: Begin Phase 4 — create `hosts/nandi/default.nix`, wire `extraModules`
in `flake.nix:107`, `git rm hosts/garuda/default.nix`, collapse garuda's `extraModules` block.

**Current State**: Phases 1-3 completed and committed (3 separate phase commits: nix.md scoping,
system/home aggregators + rewritten root import lists, discord-bot.nix options+mkIf conversion).
Plan file `specs/086_module_convention_discord_bot_opt_in/plans/01_module-convention-opt-in.md`
is current with all wave-1 checkboxes marked `[x]` and phase headings `[COMPLETED]`.

**Key Decisions Made**:
- `.claude/rules/nix.md` is gitignored in this repo (`/.claude/` in `.gitignore`) — the Phase 1
  edit is on disk but not git-tracked; only the plan-file checkpoint was committed for phase 1.
- Aggregator entry counts: system aggregator has 11 entries (matches plan), home aggregator has
  31 entries (plan estimated 27 — an approximation; confirmed 1:1 match against the original
  `home.nix` list via diff, so no drift).
- `discord-bot.nix`'s `config = lib.mkIf cfg.enable { ... };` wraps the body at existing
  indentation (not re-indented 2 further spaces) — internals are otherwise byte-for-byte
  unchanged, matching the plan's "unchanged internally" instruction.

**Deviations from Plan**:
- Phase 2 home aggregator entry count noted as 31 (not 27) — annotated inline in the plan
  checklist as `(deviation: altered — plan's entry count of 27 was an approximation; actual
  count is 31, confirmed by diff to match 1:1)`. Not a real deviation from intent, just a
  corrected count.

**What NOT to Try**:
- Do not run `nix flake check` yet expecting it to see the new/moved files unless they are
  staged — `flake.nix` uses `root = self`, and git flakes only see git-tracked (staged) content.
  Phase 6 explicitly re-stages everything before verification; the phase 2/3 git commits already
  performed above mean the tree is fully staged AND committed as of this handoff, so this is not
  currently a risk, but re-check before verification if further edits land unstaged.
- MCP-NixOS tools are unavailable in this environment (ToolSearch found no `mcp__nixos__nix`
  entries) — proceeding with `nix-instantiate --parse` / `nix flake check` only, no MCP
  package/option validation was performed (informational, not a blocker).

**References**: Plan path `specs/086_module_convention_discord_bot_opt_in/plans/01_module-convention-opt-in.md`.
Current phase: 3 completed, next is phase 4 (wave 2).
