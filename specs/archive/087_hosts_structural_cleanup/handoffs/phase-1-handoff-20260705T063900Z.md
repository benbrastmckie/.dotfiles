# Phase 1 Handoff: Baseline Capture

**Immediate Next Action**: Begin Phase 2 — rewrite `hosts/README.md`'s Usage section (lines 28-37)
to document the `mkHost` factory pattern, and refresh the Structure section.

**Current State**: Phase 1 completed. Plan file is current (`[COMPLETED]`).

**Key Decisions Made**:
- Baseline `nix flake check --no-build` confirmed: "all checks passed!" with exactly the two
  pre-existing `boot.zfs.forceImportRoot` warnings on `iso` and `usb-installer`.
- Baseline `iso` `drvPath` (for Phase 3's optional equivalence proof):
  `/nix/store/3vnp20n5d5w97da6kxgkz00d56li2cpn-nixos-system-nixos-iso-26.05.20260622.3426825.drv`

**Deviations from Plan**:
- Plain `nix eval --raw .#nixosConfigurations.iso.config.system.build.toplevel.drvPath` fails
  with "Refusing to evaluate package 'zfs-kernel-...' ... broken" — forcing `.drvPath` strictly
  evaluates the derivation (unlike `nix flake check`, which does not force it). This is task 68's
  pre-existing broken zfs-kernel state; explicitly out of scope to fix. Worked around read-only
  via `NIXPKGS_ALLOW_BROKEN=1 nix eval --impure --raw ...` — an ephemeral env var for this eval
  invocation only, no tracked file touched. Same workaround must be reused for the Phase 3
  "after" comparison, if Phase 3 is attempted.

**What NOT to Try**:
- Do not attempt plain `nix eval ... drvPath` without `NIXPKGS_ALLOW_BROKEN=1 --impure` — it will
  fail on the pre-existing broken zfs-kernel package, unrelated to this task.
- Do not touch `nixpkgs.config`/`problems.handlers` in any tracked file to permanently silence the
  broken-package check — that would touch task 68's zfs-kernel state, which is out of scope.

**References**: Plan at `specs/087_hosts_structural_cleanup/plans/01_hosts-structural-cleanup.md`,
currently at Phase 1 complete / Phase 2 next.
