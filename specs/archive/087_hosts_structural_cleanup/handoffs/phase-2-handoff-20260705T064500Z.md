# Phase 2 Handoff: hosts/README.md mkHost Factory Rewrite

**Immediate Next Action**: Attempt Phase 3 (optional) — extract the inline ISO module from
`flake.nix` (lines 138-166) to `hosts/iso/default.nix`. The Phase 1 baseline drvPath workaround
(`NIXPKGS_ALLOW_BROKEN=1 nix eval --impure --raw ...`) is confirmed to work, making the
before/after equivalence proof feasible. If the "after" drvPath does not match
`/nix/store/3vnp20n5d5w97da6kxgkz00d56li2cpn-nixos-system-nixos-iso-26.05.20260622.3426825.drv`,
revert Phase 3 and stop — Phase 2 alone is a complete, shippable deliverable.

**Current State**: Phase 2 completed and committed (`d0cc43a`). Plan file is current
(`[COMPLETED]` on both Phase 1 and Phase 2).

**Key Decisions Made**:
- `hosts/README.md` Usage section now documents the `mkHost` factory with both the simple
  one-liner form and the richer `usb-installer` form (`extraModules` + `extraSpecialArgs`).
- Added one clarifying sentence that `iso` is NOT built via `mkHost` (lacks
  `hardware-configuration.nix`) — this is in the Usage section, not the Hosts list, so it does not
  violate the "leave Available hosts / Hosts section unchanged until Phase 3" constraint.
- Structure section now documents optional `default.nix` and `README.md` per-host files.

**Deviations from Plan**: None beyond the Phase 1 drvPath workaround (already recorded in Phase
1's checklist annotations).

**What NOT to Try**:
- Do not add `iso` to the "Available hosts" line or "Hosts" section in Phase 2 — reserved for
  Phase 3 only, to keep the README self-consistent if Phase 3 is skipped.
- Do not touch task 68's broken zfs-kernel state or `lib/mkHost.nix` internals.

**References**: Plan at `specs/087_hosts_structural_cleanup/plans/01_hosts-structural-cleanup.md`,
Phase 1 and 2 complete, Phase 3 (optional) next.
