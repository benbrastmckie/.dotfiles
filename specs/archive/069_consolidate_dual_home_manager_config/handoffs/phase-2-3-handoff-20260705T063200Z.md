# Handoff: Task 69, Phases 2-3 Complete

## Immediate Next Action

Proceed to Phase 4: full re-verification (nix eval all paths, `nix flake check`, builds) and
scoped commit.

## Current State

Phase 1 (baseline), Phase 2 (specialArgs unification), and Phase 3 (doc correction) are all
`[COMPLETED]` in the plan file. `nix flake check` already re-run after Phase 2/3 edits and passed
green. Post-edit `nix eval` spot-checks confirm `nandi`/`hamsa`/`garuda`/`iso` and standalone all
show `lectic-0.0.0` in `home.packages`.

## Key Decisions Made

- Applied the identical pre-existing resolution expression
  (`lectic.packages.${system}.lectic or lectic.packages.${system}.default or lectic`) at both
  raw-lectic sites: `lib/mkHost.nix` (`home-manager.extraSpecialArgs`) and `flake.nix`
  (`hmExtraSpecialArgs`).
- For the now-redundant standalone `// { lectic = ...; }` override in `flake.nix`, chose to
  **remove** it (smaller diff) rather than keep it with an updated comment, since
  `hmExtraSpecialArgs` now already carries the resolved value. Left a short comment explaining
  why no per-path override remains.
- Rewrote the `docs/dual-home-manager.md` "extraSpecialArgs divergence (intentional)" bullet to
  describe the unified state and reference the expression by name, not by line numbers.

## Deviations from Plan

- None. Line numbers in the plan (e.g. `lib/mkHost.nix:48`) were off by one from the live repo
  (actual `inherit lectic;` was at line 47) — re-verified live before editing per task
  instructions; this is not a deviation from the plan's intent, just a stale line-number
  reference in the plan text itself.

## What NOT to Try

- Do not touch `hosts/nandi/default.nix`, `modules/*/default.nix`, `useGlobalPkgs`, or
  `useUserPackages` — explicitly out of scope (task 86 territory).
- Do not re-add the standalone `lectic` override in `flake.nix` — it is now redundant since
  `hmExtraSpecialArgs` carries the resolved value for both paths.

## References

- Plan: `specs/069_consolidate_dual_home_manager_config/plans/01_unify-lectic-specialargs.md`
- Current phase: 3 of 4 complete; Phase 4 (verification + commit) remains.
