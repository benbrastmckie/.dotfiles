# Phase 5 Handoff — Task 101

**Immediate Next Action**: Begin Phase 6 (hand-collapse modules/system/desktop.nix's `services`
(7 occurrences) and `programs` (4 occurrences), the single hardest file in this cleanup).

**Current State**: Phases 1-5 completed. `nix flake check` green. `statix check` down to 6
findings: desktop.nix (2 W20 findings: services + programs) and the 4 hardware-config files
(Phase 7 territory). `deadnix --exclude 'hosts/*/hardware-configuration.nix' . -o json` = 0.

**Key Decisions Made**:
- statix's repeated-keys rule only fires at 3+ occurrences of the same key at the same level —
  confirmed twice: power.nix's `services.earlyoom` was deliberately left unmerged (only 3 of 4
  services.* occurrences folded), dropping the file to 2 occurrences and silencing the rule.
- Research report's Tier-2 occurrence counts were undercounts in two places (dotfiles.nix: 5 not
  3 home.* occurrences; power.nix: 4 not 3 services.* occurrences) — always verify against a live
  `statix check` on the file before assuming the plan's named sub-keys are exhaustive.

**Deviations from Plan**: see Phase 4/5 plan-file annotations (dotfiles.nix folds 2 extra
`home.activation.*` entries; power.nix leaves `services.earlyoom` unmerged by design).

**What NOT to Try**: Don't assume the Tier 2/3 occurrence lists in the research report are
exhaustive — always cross-check with the live `statix check` output for the specific file before
finalizing a collapse.

**References**: Plan at specs/101_nix_lint_findings_cleanup/plans/01_lint-findings-cleanup.md,
currently after Phase 5.
